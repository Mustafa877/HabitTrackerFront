# Proxy API requests to backend to avoid Mixed Content error
/api/*  https://habit-api.mustafadev.com/api/:splat  200

# Fallback for SPA routing (optional but good practice for static exports on Netlify)
/*  /index.html  200
